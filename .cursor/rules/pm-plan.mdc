---
description: pm-plan.yaml 与 Gitee 同步脚本的对照、编辑与 Issue 契约
globs:
  - scripts/gitee-sync/pm-plan.yaml
  - scripts/gitee-sync/**/*.mjs
alwaysApply: false
---

# pm-plan 工程对照与编辑（`scripts/gitee-sync/pm-plan.yaml`）

## 角色

以**仓库内规范文档与代码事实**为参照，对 **`pm-plan.yaml`** 做对照分析，并在需要时**修改该文件**，使规划条目与实现、规范一致。本规则**不绑定具体业务领域**；产品语义由项目内 OpenSpec、`modules` 描述与里程碑自行定义。

## 对照来源（按优先级）

1. **规范文档**：如 `openspec/specs/**`；进行中的变更目录（`tasks.md`、`proposal.md` 等，若存在）。
2. **工程实现**：可核验的入口（服务、路由、构建与部署相关配置）、健康检查、与规划相关的真实代码路径；区分「已落地」与「占位」。
3. **当前** `scripts/gitee-sync/pm-plan.yaml`：作为本次修订的基线。

## 分析要点

- **里程碑** `milestones`：`title`、`description`、`due_on` 是否与阶段目标一致；是否重复、空泛或与规范脱节。
- **issues**：是否与规范或代码中的工作包对应；**`milestone`** 是否属于正确阶段；**`module`** 是否在 `modules` 列表中有定义；**`priority`** 是否与 **`priority_definitions`** 语义一致；**`body`** 是否达到「可独立评审」（见下节）。
- **缺口与冗余**：规范或代码中已存在的能力是否在条目中体现；条目中是否出现规范与代码均无依据的内容（应删改）。

## 编辑动作

- 在对比结论基础上**增删改** **`meta`、`modules`（仅当范围变化）、`milestones`、`issues`**；**勿删**契约结构节名（见「禁止」）。
- 修改后 YAML 须合法，并符合本节字段约定。

## 优先级

- Issue 的 **`priority`** 使用 **`priority_definitions`** 中的五档中文，与 **`gitee_priority.api_numeric`** 一致。
- **禁止**在 `priority` 中写 P0/P1/P2 等别名。

## issues 范围

- **待跟踪**：**`state` 为 `open` 或 `progressing`**。
- **已完结/已拒绝**：将对应条目的 **`state` 改为 `closed` 或 `rejected`**，**保留**该 Issue 的 `title`、`body`、`gitee_number` 等字段，便于与 `scripts/gitee-sync` 推送及远端对照；**勿**因完结而删除整条 Issue。

## Issue 条目

- **`meta`**：仅 **`product`**（若使用）。
- **`title`**：短句概括，**不要**带里程碑前缀；阶段用 **`milestone`**。
- **`milestone`**：与 `milestones[].title` 完全一致。
- **`module`** + **`classification`**：须在 `modules` 中有对应模块名；**`classification`** 为 **`gitee_content_classifications`** 中的英文键。
- **`gitee_number`**：与远端 Issue 编号一致（若已绑定）。
- **`state`**：**`open`、`progressing`、`closed`、`rejected`**；与 Gitee 及 `scripts/gitee-sync/index.mjs` 中 `ALLOWED_ISSUE_STATES` 一致。
- **`body`**：**不得**仅有一句口号或单条模糊描述。应写清：
  - **验收标准**或**完成定义**（可多条，建议列表）；
  - 必要时 **范围/非目标**、**依赖**（前置 spec、服务、接口）；
  - 可引用规范路径（如 `openspec/specs/...`），不重复里程碑标题或与 **`title`** 同义反复。

## 内容分类（权威定义）

`gitee_content_classifications` 为与远端一致的键列表；下列语义以本节约束为准（改定义只改本节）。

- **bug**：缺陷或异常行为，需修复。
- **duplicate**：与已有 Issue 重复。
- **enhancement**：对现有能力的优化或改进。
- **feature**：新能力需求。
- **invalid**：不成立或无法复现。
- **question**：待澄清的疑问。
- **wontfix**：已确认不修复。

## 禁止

- 在 `pm-plan.yaml` 中写入令牌、密钥或凭据。
- 删除 **`gitee_priority`、`gitee_content_classifications`** 等契约节名（列表内容可与远端策略对齐，**分类语义**改本节）。
