## 1. Backend: 通知合同补强与一致性

- [x] 1.1 将通知列表排序实现收敛为稳定键 `(CreatedAtUtc desc, Id desc)`（或等价），并补充前端去重所需字段（若缺）。
- [x] 1.2 将单条标已读实现收敛为“首次已读时间”语义：仅在 `ReadAtUtc` 为空时写入时间戳。
- [x] 1.3 将全部标已读实现收敛为“首次已读时间”语义：仅更新未读行；重复调用保持时间戳不变。
- [x] 1.4 校验“越权标已读”的统一策略（404/403）与项目既有端点一致，并在通知端点保持一致。

## 2. Backend: 回复触发通知与幂等

- [x] 2.1 校验“他人回复触发通知”覆盖：收件人取 `post.AuthorSubId`，行为人取 reply 作者 `sub`，并确保 `RecipientSubId` 与 token `sub` 语义一致。
- [x] 2.2 校验“自回复不通知”覆盖：`post.AuthorSubId == replyAuthorSubId` 时不写入通知。
- [x] 2.3 校验通知幂等：以 `ReplyId` 唯一索引阻止重复写入，并对重复写入的行为进行可观测日志记录。

## 3. Frontend: 通知列表与未读数

- [x] 3.1 新增通知列表页面：支持分页、未读筛选、空态与错误态展示。
- [x] 3.2 新增未读数展示入口（顶栏/个人中心入口择一）：拉取 `unread-count` 并格式化显示。
- [x] 3.3 新增“标为已读”交互：单条标已读与全部标已读，并在本地列表中同步状态。
- [x] 3.4 增量刷新策略：刷新第一页时按通知 `Id` 去重合并，避免重复渲染。

## 4. Frontend: 深链跳转与回复定位

- [x] 4.1 定义通知深链路由约定（`PostId` 必填、`ReplyId` 可选），并在点击通知时跳转到帖子详情。
- [x] 4.2 在帖子详情页实现 `ReplyId` 定位：加载回复列表后滚动定位并短暂高亮；找不到时回退到顶部并展示占位提示。

## 5. Tests: 端到端最小回归

- [x] 5.1 后端集成测试：A 回复 B 帖子后，B 的通知列表新增一条 `ReplyToPost`，字段包含 `PostId`、`ReplyId`、`ActorId`，且未读数增加。
- [x] 5.2 后端集成测试：B 自己回复自己的帖子不产生通知。
- [x] 5.3 后端集成测试：标已读与全标已读具备幂等语义（重复调用保持首次 `ReadAtUtc` 不变）。
