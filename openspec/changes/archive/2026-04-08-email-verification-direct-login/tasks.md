## 1. User API：验证完成即签发会话

- [x] 1.1 确认当前邮箱验证完成入口（HTTP 方法、路由、是否重定向）；在标记邮箱已验证且 artifact 作废后调用与登录相同的令牌签发/刷新会话写入逻辑
- [x] 1.2 响应体（或约定重定向/一次性交换）与登录成功对齐：同一 DTO/字段名、统一 `ApiResult` 包装与错误码
- [x] 1.3 未验证、过期、重放、限流等路径不得误发完整业务令牌；补充或调整单测/手工检查清单

## 2. 前端：成功页等同登录态

- [x] 2.1 验证成功路由解析令牌（与登录成功写入 Pinia/本地存储的路径一致），更新 axios 默认头或等价机制
- [x] 2.2 成功确认后跳转已登录壳层或首页，移除「必须再去登录页输密码」的硬编码跳转
- [x] 2.3 与 remember-me / 刷新策略对齐（与登录成功相同持久化行为）

## 3. 验证与回归

- [x] 3.1 走通：注册 → 邮件链接 → 成功后已登录并可访问受保护路由（无需再登录）
- [x] 3.2 回归：未验证账号、错误链接、过期、限流、仅刷新令牌等既有行为未破坏
