## 1. 身份声明规范与配置

- [x] 1.1 在用户服务令牌签发路径中统一 `sub` 为主标识，并明确 `userId`（若下发）必须与 `sub` 完全一致
- [x] 1.2 在共享鉴权解析逻辑中落实 `sub` 主标识读取规则，禁止业务直接信任客户端传入的 userId

## 2. 登录与刷新令牌链路

- [x] 2.1 实现邮箱登录签发 access/refresh 令牌，并确保两类令牌身份主键一致
- [x] 2.2 实现 refresh token 轮换策略，刷新成功后使旧 refresh 会话失效

## 3. Redis 黑名单与拒绝语义

- [x] 3.1 接入 Redis 黑名单/吊销存储，刷新前强制校验吊销状态
- [x] 3.2 在 `sub` 缺失或 `userId != sub` 场景统一返回 401，并对失败原因做可观测记录

## 4. 验证与联调

- [x] 4.1 补充登录、刷新、吊销、声明不一致四类接口验证用例（手工或自动化）
- [x] 4.2 前后端联调确认多服务中用户标识解析一致，仅以 `sub` 作为最终用户唯一键
