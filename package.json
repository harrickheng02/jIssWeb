{
  "name": "jissweb",
  "private": true,
  "scripts": {
    "pm:ci": "npm ci --prefix scripts/github-sync && npm ci --prefix scripts/repo-knowledge-router",
    "pm:pull": "npm run pm-pull --prefix scripts/github-sync && npm run graph:refresh",
    "pm:push": "npm run graph:verify && npm run pm-push --prefix scripts/github-sync",
    "pm:publish": "npm run graph:refresh && npm run pm:push",
    "pm:dry": "npm run pm-dry --prefix scripts/github-sync",
    "graph:refresh": "npm run refresh --prefix scripts/repo-knowledge-router",
    "graph:build": "npm run build --prefix scripts/repo-knowledge-router",
    "graph:route": "npm run route --prefix scripts/repo-knowledge-router --",
    "graph:verify": "npm run verify --prefix scripts/repo-knowledge-router",
    "graph:semantic-index": "npm run semantic-index --prefix scripts/repo-knowledge-router",
    "graph:semantic-search": "npm run semantic-search --prefix scripts/repo-knowledge-router --"
  }
}
